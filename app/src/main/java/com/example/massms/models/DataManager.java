package com.example.massms.models;

public class DataManager {
    // Here we will store any data our app will use
    // since out app is so small we probably only need one "DataManager" type class
}
