package com.example.massms;

public class DependencyInjectorImplementation implements DependencyInjector {
    // Not sure if we really have dependencies that need to be injected
    // Examples of dependencies would be a database or a repository of some kind
}
